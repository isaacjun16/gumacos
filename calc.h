#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

char* calc(char* command, char* parameters) {
  strcmp(command, parameters);
  return "metodo de calculadora, ejemplo de como se debe recibir los parametros y retornar la respuestas.\n";
}
